services:
  mysql:
    image: mysql:8
    container_name: hr-management-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_DATABASE: hr_management
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - mysql_data:/var/lib/mysql
    restart: unless-stopped

volumes:
  mysql_data:
